<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
<title>Spinzamo: Start With Free Coins | Play Slots-Style Games for Fun</title>
<meta name="description" content="Join Spinzamo and start with 50,000 Gold Coins + 50 free Spinzamo Coins. No purchase necessary. Play slots-style games for fun and redeem Spinzamo Coins for real prizes. 21+." />

<!-- ============================================================
     SPINZAMO BROAD CAMPAIGN LANDING PAGE
     Goal: maximize qualified registrations, primed for first purchase (FTD).
     Single file. No external images. No browser storage (session state only).
     Configure the CONFIG block in the <script> at the bottom.
     ============================================================ -->

<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@600;700;800;900&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />

<style>
  :root{
    --bg-0:#140726;
    --bg-1:#22104a;
    --bg-2:#311663;
    --panel:#2a1456;
    --panel-2:#371a6e;
    --purple:#6d28d9;
    --purple-2:#7c3aed;
    --sweeps:#a855f7;
    --sweeps-2:#c084fc;
    --gold-0:#ffe07a;
    --gold-1:#ffbb2e;
    --gold-2:#ff9012;
    --orange-1:#ff7a1a;
    --orange-2:#ff4d17;
    --green-1:#2bd86b;
    --green-2:#16b85a;
    --ink:#ffffff;
    --muted:rgba(255,255,255,.74);
    --muted-2:rgba(255,255,255,.55);
    --line:rgba(196,132,252,.20);
    --line-strong:rgba(196,132,252,.42);
    --card:#33186a;
    --card-edge:rgba(196,132,252,.28);
    --shadow:0 18px 50px rgba(8,3,20,.55);
    --radius:20px;
    --maxw:1140px;
  }

  *{box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  body{
    margin:0;
    font-family:"Inter",system-ui,-apple-system,sans-serif;
    color:var(--ink);
    background:
      radial-gradient(1100px 700px at 50% -8%, #4a1f93 0%, rgba(74,31,147,0) 60%),
      radial-gradient(900px 600px at 100% 12%, #5a2a8e 0%, rgba(90,42,142,0) 55%),
      linear-gradient(180deg, var(--bg-1) 0%, var(--bg-0) 100%);
    background-attachment:fixed;
    line-height:1.55;
    -webkit-font-smoothing:antialiased;
    overflow-x:hidden;
  }
  a{color:inherit;}
  img{max-width:100%;display:block;}

  /* ---------- layout ---------- */
  .wrap{max-width:var(--maxw);margin:0 auto;padding:0 22px;}
  .sec{padding:74px 0;position:relative;}
  .sec--tight{padding:48px 0;}
  @media(max-width:640px){.sec{padding:52px 0;} .sec--tight{padding:34px 0;}}

  .eyebrow{
    display:inline-flex;align-items:center;gap:8px;
    font-family:"Poppins";font-weight:700;font-size:.74rem;letter-spacing:.16em;
    text-transform:uppercase;color:var(--sweeps-2);
    background:rgba(168,85,247,.12);border:1px solid var(--line-strong);
    padding:7px 14px;border-radius:999px;
  }
  .h2{
    font-family:"Poppins";font-weight:800;line-height:1.04;letter-spacing:-.01em;
    font-size:clamp(1.9rem,5.2vw,3rem);margin:18px 0 14px;
  }
  .lead{font-size:clamp(1rem,2.4vw,1.15rem);color:var(--muted);max-width:620px;}
  .gold-text{
    background:linear-gradient(180deg,var(--gold-0),var(--gold-2));
    -webkit-background-clip:text;background-clip:text;color:transparent;
  }
  .center{text-align:center;}
  .center .lead{margin-left:auto;margin-right:auto;}

  /* ---------- wordmark ---------- */
  .wordmark{
    font-family:"Poppins";font-weight:900;letter-spacing:.01em;
    line-height:1;text-transform:uppercase;display:inline-block;
    background:linear-gradient(180deg,#fff 4%,var(--gold-0) 30%,var(--gold-1) 62%,var(--gold-2) 100%);
    -webkit-background-clip:text;background-clip:text;color:transparent;
    filter:drop-shadow(0 3px 0 #7a3a06) drop-shadow(0 6px 14px rgba(0,0,0,.45));
  }

  /* ---------- buttons ---------- */
  .btn{
    display:inline-flex;align-items:center;justify-content:center;gap:10px;
    font-family:"Poppins";font-weight:800;font-size:1.06rem;letter-spacing:.01em;
    border:0;border-radius:14px;cursor:pointer;text-decoration:none;
    padding:17px 30px;color:#fff;width:auto;
    background:linear-gradient(180deg,var(--orange-1),var(--orange-2));
    box-shadow:0 10px 24px rgba(255,77,23,.34), inset 0 1px 0 rgba(255,255,255,.34);
    transition:transform .12s ease, box-shadow .12s ease, filter .12s ease;
    -webkit-tap-highlight-color:transparent;
  }
  .btn:hover{transform:translateY(-2px);box-shadow:0 16px 30px rgba(255,77,23,.42), inset 0 1px 0 rgba(255,255,255,.34);}
  .btn:active{transform:translateY(0);}
  .btn--block{width:100%;}
  .btn--xl{padding:19px 34px;font-size:1.16rem;}
  .btn--ghost{
    background:rgba(255,255,255,.05);border:1px solid var(--line-strong);
    box-shadow:none;color:#fff;font-weight:700;
  }
  .btn--ghost:hover{background:rgba(255,255,255,.09);box-shadow:none;}

  /* ---------- coins / ambient ---------- */
  .coin-svg{width:100%;height:100%;display:block;}
  .ambient{position:absolute;inset:0;overflow:hidden;pointer-events:none;z-index:0;}
  .fall{position:absolute;top:-60px;width:46px;height:46px;opacity:.85;
    animation:fall linear infinite;filter:drop-shadow(0 6px 10px rgba(0,0,0,.35));}
  @keyframes fall{
    0%{transform:translateY(-60px) rotate(0deg);opacity:0;}
    8%{opacity:.9;}
    100%{transform:translateY(115vh) rotate(420deg);opacity:.15;}
  }
  @media(prefers-reduced-motion:reduce){.fall{display:none;} html{scroll-behavior:auto;}}

  /* ---------- HERO ---------- */
  .hero{position:relative;padding:30px 0 64px;z-index:1;}
  .hero-top{display:flex;justify-content:center;padding:12px 0 6px;}
  .hero-top .wordmark{font-size:clamp(2rem,7vw,3.1rem);}
  .hero-grid{
    display:grid;grid-template-columns:1.05fr .95fr;gap:40px;align-items:center;
    margin-top:26px;position:relative;z-index:1;
  }
  @media(max-width:920px){.hero-grid{grid-template-columns:1fr;gap:28px;}}

  .hero h1{
    font-family:"Poppins";font-weight:900;line-height:.98;letter-spacing:-.015em;
    font-size:clamp(2.6rem,7.4vw,4.4rem);margin:6px 0 0;text-transform:uppercase;
  }
  .hero h1 .l1{color:#fff;display:block;}
  .hero h1 .l2{
    display:block;
    background:linear-gradient(180deg,var(--gold-0) 8%,var(--gold-2) 100%);
    -webkit-background-clip:text;background-clip:text;color:transparent;
    filter:drop-shadow(0 2px 0 #7a3a06);
  }
  .hero-sub{font-size:clamp(1.02rem,2.6vw,1.2rem);color:var(--muted);margin:18px 0 0;max-width:520px;}
  .hero-chips{display:flex;flex-wrap:wrap;gap:10px;margin:22px 0 0;}
  .chip{
    display:inline-flex;align-items:center;gap:8px;font-size:.82rem;font-weight:600;
    color:var(--muted);background:rgba(255,255,255,.05);
    border:1px solid var(--line);padding:8px 13px;border-radius:999px;
  }
  .chip svg{width:15px;height:15px;flex:0 0 auto;}

  /* offer card */
  .offer-card{
    background:linear-gradient(180deg,var(--card) 0%,var(--panel) 100%);
    border:1px solid var(--card-edge);border-radius:24px;
    box-shadow:var(--shadow);padding:26px 24px;position:relative;overflow:hidden;
  }
  .offer-card::before{
    content:"";position:absolute;inset:0;border-radius:24px;padding:1px;pointer-events:none;
    background:linear-gradient(180deg,rgba(196,132,252,.5),rgba(196,132,252,0) 45%);
    -webkit-mask:linear-gradient(#000 0 0) content-box,linear-gradient(#000 0 0);
    -webkit-mask-composite:xor;mask-composite:exclude;
  }
  .offer-flag{
    position:absolute;top:14px;right:-34px;transform:rotate(34deg);
    background:linear-gradient(180deg,var(--green-1),var(--green-2));
    color:#06210f;font-family:"Poppins";font-weight:800;font-size:.66rem;letter-spacing:.08em;
    text-transform:uppercase;padding:6px 42px;box-shadow:0 6px 14px rgba(0,0,0,.3);
  }
  .offer-label{font-family:"Poppins";font-weight:700;font-size:.74rem;letter-spacing:.14em;
    text-transform:uppercase;color:var(--sweeps-2);text-align:center;}
  .offer-split{display:grid;grid-template-columns:1fr auto 1fr;align-items:center;gap:6px;margin:14px 0 4px;}
  .offer-col{text-align:center;padding:8px 4px;}
  .offer-coin{width:54px;height:54px;margin:0 auto 8px;}
  .offer-num{font-family:"Poppins";font-weight:900;font-size:clamp(1.5rem,4.6vw,2rem);line-height:1;}
  .offer-num.gold{color:var(--gold-0);}
  .offer-num.sweep{color:var(--sweeps-2);}
  .offer-unit{font-size:.78rem;color:var(--muted);font-weight:600;margin-top:5px;display:block;}
  .offer-sub{font-size:.7rem;color:var(--muted-2);margin-top:3px;display:block;}
  .offer-amp{font-family:"Poppins";font-weight:800;color:var(--sweeps-2);font-size:1.2rem;opacity:.8;}

  /* form */
  .form{margin-top:18px;}
  .field{margin-bottom:11px;}
  .field label{display:block;font-size:.78rem;font-weight:600;color:var(--muted);margin-bottom:5px;}
  .input,.select{
    width:100%;font-family:"Inter";font-size:1rem;color:#fff;
    background:rgba(8,3,20,.42);border:1px solid var(--line-strong);border-radius:12px;
    padding:13px 14px;outline:none;transition:border-color .15s ease,box-shadow .15s ease;
  }
  .input::placeholder{color:rgba(255,255,255,.4);}
  .input:focus,.select:focus{border-color:var(--sweeps);box-shadow:0 0 0 3px rgba(168,85,247,.25);}
  .select{appearance:none;background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='12' height='8' viewBox='0 0 12 8'><path d='M1 1l5 5 5-5' stroke='%23c084fc' stroke-width='2' fill='none' stroke-linecap='round'/></svg>");background-repeat:no-repeat;background-position:right 14px center;padding-right:38px;}
  .dob-row{display:grid;grid-template-columns:1fr 1fr 1.1fr;gap:8px;}
  .form-note{font-size:.72rem;color:var(--muted-2);margin-top:10px;text-align:center;line-height:1.5;}
  .form-note a{color:var(--sweeps-2);text-decoration:underline;text-underline-offset:2px;}
  .err{color:#ff9a9a;font-size:.78rem;margin-top:8px;display:none;}
  .err.show{display:block;}

  .form-success{display:none;text-align:center;padding:8px 4px;}
  .form-success.show{display:block;}
  .form-success .tick{width:62px;height:62px;margin:4px auto 12px;}
  .form-success h3{font-family:"Poppins";font-weight:800;font-size:1.4rem;margin:0 0 6px;}
  .form-success p{color:var(--muted);font-size:.94rem;margin:0 0 16px;}

  /* ---------- trust strip ---------- */
  .trust{border-top:1px solid var(--line);border-bottom:1px solid var(--line);background:rgba(8,3,20,.28);}
  .trust-row{display:grid;grid-template-columns:repeat(4,1fr);gap:18px;}
  @media(max-width:760px){.trust-row{grid-template-columns:repeat(2,1fr);gap:14px;}}
  .trust-item{display:flex;align-items:center;gap:11px;}
  .trust-item svg{width:26px;height:26px;flex:0 0 auto;color:var(--sweeps-2);}
  .trust-item b{font-family:"Poppins";font-weight:700;font-size:.92rem;display:block;}
  .trust-item span{font-size:.76rem;color:var(--muted-2);}

  /* ---------- how it works ---------- */
  .steps{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-top:34px;}
  @media(max-width:820px){.steps{grid-template-columns:1fr;}}
  .step{
    background:linear-gradient(180deg,var(--card),var(--panel));
    border:1px solid var(--card-edge);border-radius:var(--radius);padding:26px 22px;position:relative;
  }
  .step-n{
    font-family:"Poppins";font-weight:900;font-size:.9rem;color:#fff;
    width:34px;height:34px;border-radius:10px;display:flex;align-items:center;justify-content:center;
    background:linear-gradient(180deg,var(--purple-2),var(--purple));margin-bottom:16px;
  }
  .step h3{font-family:"Poppins";font-weight:800;font-size:1.18rem;margin:0 0 8px;}
  .step p{color:var(--muted);font-size:.94rem;margin:0;}
  .step .pill{display:inline-block;margin-top:12px;font-size:.74rem;font-weight:600;
    color:var(--sweeps-2);background:rgba(168,85,247,.12);border:1px solid var(--line);
    padding:5px 11px;border-radius:999px;}

  /* ---------- games ---------- */
  .games-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;margin-top:34px;}
  @media(max-width:900px){.games-grid{grid-template-columns:repeat(2,1fr);}}
  .game{
    border-radius:18px;overflow:hidden;border:1px solid var(--card-edge);
    aspect-ratio:3/4;position:relative;display:flex;align-items:flex-end;
    box-shadow:0 12px 26px rgba(8,3,20,.4);
    transition:transform .15s ease;
  }
  .game:hover{transform:translateY(-4px);}
  .game-art{position:absolute;inset:0;}
  .game-name{
    position:relative;width:100%;padding:34px 14px 14px;
    font-family:"Poppins";font-weight:800;font-size:1.02rem;line-height:1.1;
    background:linear-gradient(180deg,rgba(10,4,24,0),rgba(10,4,24,.85));
  }
  .game-tag{position:absolute;top:10px;left:10px;font-size:.64rem;font-weight:700;letter-spacing:.06em;
    text-transform:uppercase;color:#fff;background:rgba(8,3,20,.55);border:1px solid var(--line);
    padding:4px 8px;border-radius:999px;backdrop-filter:blur(2px);}

  /* ---------- why / benefits ---------- */
  .why-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-top:34px;}
  @media(max-width:820px){.why-grid{grid-template-columns:1fr;}}
  .why{
    background:rgba(255,255,255,.035);border:1px solid var(--line);border-radius:var(--radius);
    padding:24px 22px;
  }
  .why-ic{width:46px;height:46px;border-radius:12px;display:flex;align-items:center;justify-content:center;
    background:linear-gradient(180deg,rgba(255,187,46,.18),rgba(255,144,18,.1));
    border:1px solid rgba(255,187,46,.3);margin-bottom:14px;}
  .why-ic svg{width:24px;height:24px;color:var(--gold-0);}
  .why h3{font-family:"Poppins";font-weight:800;font-size:1.12rem;margin:0 0 7px;}
  .why p{color:var(--muted);font-size:.92rem;margin:0;}

  /* ---------- proof ---------- */
  .proof-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-top:30px;}
  @media(max-width:820px){.proof-grid{grid-template-columns:1fr;}}
  .quote{
    background:linear-gradient(180deg,var(--card),var(--panel));
    border:1px solid var(--card-edge);border-radius:var(--radius);padding:24px 22px;
  }
  .stars{color:var(--gold-0);font-size:1rem;letter-spacing:2px;margin-bottom:12px;}
  .quote p{font-size:.96rem;margin:0 0 14px;color:#fff;}
  .quote .who{font-size:.82rem;color:var(--muted-2);font-weight:600;}

  /* ---------- first purchase / FTD primer ---------- */
  .ftd{
    background:
      radial-gradient(600px 300px at 80% 0%, rgba(168,85,247,.22), rgba(168,85,247,0) 60%),
      linear-gradient(180deg,var(--panel-2),var(--panel));
    border:1px solid var(--line-strong);border-radius:26px;padding:36px 30px;
    display:grid;grid-template-columns:1.1fr .9fr;gap:30px;align-items:center;
  }
  @media(max-width:820px){.ftd{grid-template-columns:1fr;padding:28px 22px;text-align:center;}}
  .ftd h2{font-family:"Poppins";font-weight:900;font-size:clamp(1.6rem,4.6vw,2.2rem);line-height:1.05;margin:0 0 12px;}
  .ftd p{color:var(--muted);margin:0 0 8px;}
  .ftd .micro{font-size:.78rem;color:var(--muted-2);margin-top:14px;}
  .ftd-card{
    background:rgba(8,3,20,.34);border:1px solid var(--card-edge);border-radius:18px;padding:22px;
  }
  .ftd-card .tier{display:flex;align-items:center;justify-content:space-between;gap:12px;
    padding:13px 0;border-bottom:1px dashed var(--line);}
  .ftd-card .tier:last-child{border-bottom:0;}
  .ftd-card .tier b{font-family:"Poppins";font-weight:800;}
  .ftd-card .tier .sc{color:var(--sweeps-2);font-weight:700;font-size:.86rem;}

  /* ---------- final CTA ---------- */
  .final{text-align:center;position:relative;z-index:1;}
  .final .wordmark{font-size:clamp(1.8rem,6vw,2.6rem);margin-bottom:8px;}
  .final h2{font-family:"Poppins";font-weight:900;font-size:clamp(1.9rem,5.4vw,3rem);margin:10px 0 14px;text-transform:uppercase;line-height:1;}

  /* ---------- footer ---------- */
  .footer{border-top:1px solid var(--line);background:rgba(8,3,20,.4);padding:46px 0 60px;font-size:.84rem;}
  .footer .rg{
    display:flex;flex-wrap:wrap;align-items:center;gap:14px;justify-content:center;
    background:rgba(168,85,247,.08);border:1px solid var(--line);border-radius:16px;
    padding:18px 20px;margin-bottom:26px;text-align:center;
  }
  .footer .rg b{font-family:"Poppins";font-weight:800;color:#fff;}
  .footer .rg a{color:var(--sweeps-2);font-weight:600;}
  .legal{color:var(--muted-2);line-height:1.7;max-width:880px;margin:0 auto;text-align:center;}
  .legal p{margin:0 0 12px;}
  .legal a{color:var(--muted);text-decoration:underline;text-underline-offset:2px;}
  .foot-links{display:flex;flex-wrap:wrap;gap:8px 20px;justify-content:center;margin:18px 0 6px;}
  .foot-links a{color:var(--muted);font-weight:600;}
  .age-badge{display:inline-flex;align-items:center;justify-content:center;width:30px;height:30px;border-radius:8px;
    border:2px solid var(--sweeps-2);color:var(--sweeps-2);font-family:"Poppins";font-weight:800;font-size:.72rem;}

  /* ---------- sticky mobile CTA ---------- */
  .sticky-cta{
    position:fixed;left:0;right:0;bottom:0;z-index:40;padding:10px 16px calc(10px + env(safe-area-inset-bottom));
    background:linear-gradient(180deg,rgba(20,7,38,0),rgba(20,7,38,.96) 30%);
    transform:translateY(120%);transition:transform .25s ease;display:none;
  }
  .sticky-cta.show{transform:translateY(0);}
  @media(max-width:820px){.sticky-cta{display:block;}}

  /* ---------- age gate ---------- */
  .gate{
    position:fixed;inset:0;z-index:100;display:flex;align-items:center;justify-content:center;padding:22px;
    background:rgba(8,3,20,.86);backdrop-filter:blur(6px);
  }
  .gate[hidden]{display:none;}
  .gate-card{
    max-width:430px;width:100%;background:linear-gradient(180deg,var(--card),var(--panel));
    border:1px solid var(--card-edge);border-radius:24px;box-shadow:var(--shadow);
    padding:34px 28px;text-align:center;
  }
  .gate-card .wordmark{font-size:2.1rem;margin-bottom:14px;}
  .gate-card h2{font-family:"Poppins";font-weight:800;font-size:1.45rem;margin:0 0 10px;}
  .gate-card p{color:var(--muted);font-size:.92rem;margin:0 0 22px;}
  .gate-actions{display:grid;grid-template-columns:1fr 1fr;gap:12px;}
  .gate-deny{display:none;color:var(--muted);font-size:.86rem;margin-top:18px;}
  .gate-deny.show{display:block;}
  .gate-mini{font-size:.72rem;color:var(--muted-2);margin-top:18px;line-height:1.5;}

  :focus-visible{outline:3px solid var(--sweeps-2);outline-offset:2px;border-radius:6px;}
</style>
</head>
<body>

<!-- ===================== AGE GATE ===================== -->
<div class="gate" id="ageGate" role="dialog" aria-modal="true" aria-labelledby="gateTitle">
  <div class="gate-card">
    <div class="wordmark">Spinzamo</div>
    <h2 id="gateTitle">Are you 21 or older?</h2>
    <p>Spinzamo is a free-to-play social game for adults. You must be 21+ to enter.</p>
    <div class="gate-actions">
      <button class="btn btn--ghost" id="gateNo">No</button>
      <button class="btn" id="gateYes">Yes, I’m 21+</button>
    </div>
    <div class="gate-deny" id="gateDeny">
      You must be 21 or older to visit Spinzamo. Come back when you’re of age, and remember to play responsibly.
    </div>
    <div class="gate-mini">No purchase necessary. Void where prohibited. Available in select US states only.</div>
  </div>
</div>

<!-- ===================== HERO ===================== -->
<header class="hero">
  <div class="ambient" id="heroAmbient" aria-hidden="true"></div>
  <div class="wrap">
    <div class="hero-top"><span class="wordmark">Spinzamo</span></div>
    <div class="hero-grid">
      <div>
        <span class="eyebrow">★ Free to play · Real prizes</span>
        <h1><span class="l1">Start with</span><span class="l2">free coins</span></h1>
        <p class="hero-sub">Sign up and jump into slots-style fun. New players get a welcome stack on the house. No purchase, no catch.</p>
        <div class="hero-chips">
          <span class="chip"><svg viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="2"/><path d="M12 7v10M9.5 9.5c0-1.1 1.1-1.7 2.5-1.7s2.5.6 2.5 1.7-1.1 1.6-2.5 1.6-2.5.6-2.5 1.7 1.1 1.7 2.5 1.7 2.5-.6 2.5-1.7" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/></svg>50,000 Gold Coins</span>
          <span class="chip"><svg viewBox="0 0 24 24" fill="none"><path d="M12 3l2.5 5 5.5.8-4 3.9.9 5.5L12 21.6 7.6 18.7l.9-5.5-4-3.9 5.5-.8L12 3z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"/></svg>50 Free Spinzamo Coins</span>
          <span class="chip"><svg viewBox="0 0 24 24" fill="none"><path d="M12 3l7 3v5c0 4.4-3 7.7-7 9-4-1.3-7-4.6-7-9V6l7-3z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"/><path d="M9 12l2 2 4-4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>No purchase necessary</span>
        </div>
      </div>

      <!-- OFFER + FORM -->
      <div class="offer-card" id="signup">
        <div class="offer-flag">Welcome offer</div>
        <div class="offer-label">Your free welcome stack</div>
        <div class="offer-split">
          <div class="offer-col">
            <div class="offer-coin">
              <svg class="coin-svg" viewBox="0 0 64 64"><defs><radialGradient id="gc" cx="40%" cy="32%" r="75%"><stop offset="0" stop-color="#ffe9a8"/><stop offset="55%" stop-color="#ffbb2e"/><stop offset="100%" stop-color="#e07f00"/></radialGradient></defs><circle cx="32" cy="32" r="29" fill="url(#gc)" stroke="#a85e00" stroke-width="2.5"/><circle cx="32" cy="32" r="22" fill="none" stroke="#fff6d8" stroke-width="2" opacity=".55"/><text x="32" y="43" font-family="Poppins" font-weight="900" font-size="30" text-anchor="middle" fill="#8a4b00">$</text></svg>
            </div>
            <div class="offer-num gold">50,000</div>
            <span class="offer-unit">Gold Coins</span>
            <span class="offer-sub">Play for fun</span>
          </div>
          <div class="offer-amp">+</div>
          <div class="offer-col">
            <div class="offer-coin">
              <svg class="coin-svg" viewBox="0 0 64 64"><defs><radialGradient id="sc" cx="40%" cy="32%" r="75%"><stop offset="0" stop-color="#e9d4ff"/><stop offset="55%" stop-color="#a855f7"/><stop offset="100%" stop-color="#6d28d9"/></radialGradient></defs><circle cx="32" cy="32" r="29" fill="url(#sc)" stroke="#4c1d95" stroke-width="2.5"/><circle cx="32" cy="32" r="22" fill="none" stroke="#f1e3ff" stroke-width="2" opacity=".5"/><text x="32" y="43" font-family="Poppins" font-weight="900" font-size="30" text-anchor="middle" fill="#3b1574">$</text></svg>
            </div>
            <div class="offer-num sweep">50</div>
            <span class="offer-unit">Spinzamo Coins</span>
            <span class="offer-sub">Redeem for prizes</span>
          </div>
        </div>

        <!-- FORM -->
        <form class="form" id="claimForm" novalidate>
          <div class="field">
            <label for="email">Email address</label>
            <input class="input" type="email" id="email" name="email" autocomplete="email" placeholder="you@email.com" required />
          </div>
          <div class="field">
            <label for="state">Your state</label>
            <select class="select" id="state" name="state" required>
              <option value="">Select state</option>
              <option value="FL">Florida</option>
              <option value="IL">Illinois</option>
              <option value="NC">North Carolina</option>
              <option value="OH">Ohio</option>
              <option value="TX">Texas</option>
              <option value="OTHER">Other state</option>
            </select>
          </div>
          <div class="err" id="formErr"></div>
          <button class="btn btn--block btn--xl" type="submit" id="claimBtn">Claim Free Coins</button>
          <p class="form-note">By continuing you confirm you’re 21+ and agree to the
            <a href="#footer">Terms</a>, <a href="#footer">Official Rules</a> &amp; <a href="#footer">Privacy Policy</a>.
            No purchase necessary.</p>
        </form>

        <!-- SUCCESS -->
        <div class="form-success" id="formSuccess" aria-live="polite">
          <div class="tick">
            <svg class="coin-svg" viewBox="0 0 64 64"><circle cx="32" cy="32" r="29" fill="none" stroke="#2bd86b" stroke-width="3"/><path d="M20 33l8 8 16-18" stroke="#2bd86b" stroke-width="4" fill="none" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </div>
          <h3>You’re in. Coins incoming!</h3>
          <p>Check your inbox to verify your email, then your Gold Coins and Spinzamo Coins are ready to play.</p>
          <a class="btn btn--block" id="successCta" href="#">Enter Spinzamo</a>
        </div>
      </div>
    </div>
  </div>
</header>

<!-- ===================== TRUST STRIP ===================== -->
<section class="sec sec--tight trust">
  <div class="wrap">
    <div class="trust-row">
      <div class="trust-item">
        <svg viewBox="0 0 24 24" fill="none"><path d="M12 3l7 3v5c0 4.4-3 7.7-7 9-4-1.3-7-4.6-7-9V6l7-3z" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round"/><path d="M9 12l2 2 4-4" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"/></svg>
        <div><b>No purchase necessary</b><span>Free coins, free to play</span></div>
      </div>
      <div class="trust-item">
        <svg viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.7"/><path d="M12 8v4l3 2" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"/></svg>
        <div><b>1× playthrough</b><span>Simple, fair redemption</span></div>
      </div>
      <div class="trust-item">
        <svg viewBox="0 0 24 24" fill="none"><rect x="4" y="10" width="16" height="10" rx="2" stroke="currentColor" stroke-width="1.7"/><path d="M8 10V7a4 4 0 018 0v3" stroke="currentColor" stroke-width="1.7"/></svg>
        <div><b>Secure &amp; verified</b><span>Encrypted, KYC-checked</span></div>
      </div>
      <div class="trust-item">
        <svg viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.7"/><text x="12" y="16" font-family="Poppins" font-weight="800" font-size="9" text-anchor="middle" fill="currentColor">21+</text></svg>
        <div><b>21+ only</b><span>Play responsibly</span></div>
      </div>
    </div>
  </div>
</section>

<!-- ===================== HOW IT WORKS ===================== -->
<section class="sec" id="how">
  <div class="wrap center">
    <span class="eyebrow">How Spinzamo works</span>
    <h2 class="h2">Two coins. <span class="gold-text">Zero confusion.</span></h2>
    <p class="lead">Spinzamo runs on a free-to-play sweepstakes model. Here’s the whole thing in three steps.</p>
  </div>
  <div class="wrap">
    <div class="steps">
      <div class="step">
        <div class="step-n">1</div>
        <h3>Sign up free</h3>
        <p>Create your account in under a minute and grab your welcome stack of Gold Coins plus free Spinzamo Coins. No card, no deposit.</p>
        <span class="pill">Free welcome stack</span>
      </div>
      <div class="step">
        <div class="step-n">2</div>
        <h3>Play for fun</h3>
        <p>Use <b>Gold Coins</b> to play slots-style games purely for entertainment. Top up daily with free coins, every single day.</p>
        <span class="pill">Free coins daily</span>
      </div>
      <div class="step">
        <div class="step-n">3</div>
        <h3>Redeem for prizes</h3>
        <p>Play with <b>Spinzamo Coins</b> for the chance to redeem real prizes once you clear a simple 1× playthrough. Cash and gift cards, per the Official Rules.</p>
        <span class="pill">Real prizes</span>
      </div>
    </div>
  </div>
</section>

<!-- ===================== GAMES ===================== -->
<section class="sec" id="games">
  <div class="wrap center">
    <span class="eyebrow">The lobby</span>
    <h2 class="h2">More slots. <span class="gold-text">More fun.</span></h2>
    <p class="lead">Colourful, fast-loading games built for quick breaks. New titles drop all the time.</p>
  </div>
  <div class="wrap">
    <div class="games-grid">
      <div class="game">
        <div class="game-art"><svg class="coin-svg" viewBox="0 0 120 160" preserveAspectRatio="xMidYMid slice"><defs><linearGradient id="g1" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#3b0d6e"/><stop offset="1" stop-color="#1c5fd6"/></linearGradient></defs><rect width="120" height="160" fill="url(#g1)"/><circle cx="60" cy="64" r="38" fill="#0e1b4a" opacity=".5"/><text x="60" y="84" font-family="Poppins" font-weight="900" font-size="64" text-anchor="middle" fill="#ff3b6b">7</text></svg></div>
        <span class="game-tag">Slots</span>
        <div class="game-name">Neon Spin</div>
      </div>
      <div class="game">
        <div class="game-art"><svg class="coin-svg" viewBox="0 0 120 160" preserveAspectRatio="xMidYMid slice"><defs><linearGradient id="g2" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#6e0d3b"/><stop offset="1" stop-color="#d68a1c"/></linearGradient></defs><rect width="120" height="160" fill="url(#g2)"/><path d="M40 70l20-26 20 26-20 14z" fill="#ffd75e" opacity=".9"/><circle cx="60" cy="58" r="6" fill="#fff"/></svg></div>
        <span class="game-tag">Jackpot</span>
        <div class="game-name">Royal Fortune</div>
      </div>
      <div class="game">
        <div class="game-art"><svg class="coin-svg" viewBox="0 0 120 160" preserveAspectRatio="xMidYMid slice"><defs><linearGradient id="g3" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#0d6e3b"/><stop offset="1" stop-color="#1cc06e"/></linearGradient></defs><rect width="120" height="160" fill="url(#g3)"/><circle cx="60" cy="62" r="34" fill="#0a3a22" opacity=".5"/><circle cx="48" cy="56" r="7" fill="#fff"/><circle cx="72" cy="56" r="7" fill="#fff"/><path d="M44 78q16 14 32 0" stroke="#fff" stroke-width="4" fill="none" stroke-linecap="round"/></svg></div>
        <span class="game-tag">Slots</span>
        <div class="game-name">Jungle Rush</div>
      </div>
      <div class="game">
        <div class="game-art"><svg class="coin-svg" viewBox="0 0 120 160" preserveAspectRatio="xMidYMid slice"><defs><linearGradient id="g4" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#6e1c0d"/><stop offset="1" stop-color="#d6321c"/></linearGradient></defs><rect width="120" height="160" fill="url(#g4)"/><path d="M34 84c8-22 44-22 52 0" stroke="#ffd75e" stroke-width="6" fill="none" stroke-linecap="round"/><path d="M48 52l12 8 12-8" stroke="#fff" stroke-width="4" fill="none" stroke-linecap="round"/></svg></div>
        <span class="game-tag">Slots</span>
        <div class="game-name">Dragon Legend</div>
      </div>
    </div>
  </div>
</section>

<!-- ===================== SOCIAL PROOF =====================
     NOTE: EXAMPLE reviews for layout only. Replace with real,
     opted-in player testimonials before launch. Do not attribute
     fabricated quotes to named individuals.                        -->
<section class="sec" id="proof">
  <div class="wrap center">
    <span class="eyebrow">★ Loved by players</span>
    <h2 class="h2">Why players <span class="gold-text">keep coming back.</span></h2>
  </div>
  <div class="wrap">
    <div class="proof-grid">
      <div class="quote">
        <div class="stars">★★★★★</div>
        <p>“Signed up in under a minute and the daily free coins keep me coming back. Smoothest social casino I’ve tried on my phone.”</p>
        <div class="who">Example review · FL</div>
      </div>
      <div class="quote">
        <div class="stars">★★★★★</div>
        <p>“The daily free coins are the best part. I never feel like I have to buy anything to have fun here.”</p>
        <div class="who">Example review · OH</div>
      </div>
      <div class="quote">
        <div class="stars">★★★★★</div>
        <p>“Games load instantly and look great on my phone. Easy to pick up for a quick break, easy to put down.”</p>
        <div class="who">Example review · TX</div>
      </div>
    </div>
    <p style="text-align:center;font-size:.74rem;color:var(--muted-2);margin-top:18px;">Example reviews shown for layout. Replace with real, opted-in player quotes. Individual experiences vary; prizes are subject to the Official Rules.</p>
  </div>
</section>

<!-- ===================== FIRST PURCHASE / FTD PRIMER ===================== -->
<section class="sec" id="more">
  <div class="wrap">
    <div class="ftd">
      <div>
        <span class="eyebrow">For new players</span>
        <h2>Want a bigger head start?</h2>
        <p>Once you’re in, new players unlock a one-time welcome coin pack: a stack of extra Gold Coins to play with, plus bonus Spinzamo Coins to chase real prizes.</p>
        <p class="micro">It’s always optional. You can play Spinzamo completely free, top up with free coins every day, and request free Spinzamo Coins by mail. No purchase necessary, ever.</p>
      </div>
      <div class="ftd-card" aria-label="Example welcome coin pack">
        <div class="tier"><b>Starter pack</b><span class="sc">+ bonus Spinzamo Coins</span></div>
        <div class="tier"><b>Value pack</b><span class="sc">+ more bonus Coins</span></div>
        <div class="tier"><b>Best value</b><span class="sc">+ most bonus Coins</span></div>
        <a class="btn btn--block" href="#signup" style="margin-top:16px;">Sign up to unlock</a>
      </div>
    </div>
  </div>
</section>

<!-- ===================== FINAL CTA ===================== -->
<section class="sec final">
  <div class="ambient" id="finalAmbient" aria-hidden="true"></div>
  <div class="wrap">
    <span class="wordmark">Spinzamo</span>
    <h2>Your free coins<br>are waiting</h2>
    <p class="lead" style="margin:0 auto 26px;">Join in a minute, play for fun, and redeem Spinzamo Coins for real prizes.</p>
    <a class="btn btn--xl" href="#signup">Claim Free Coins</a>
  </div>
</section>

<!-- ===================== FOOTER ===================== -->
<footer class="footer" id="footer">
  <div class="wrap">
    <div class="rg">
      <span class="age-badge">21+</span>
      <span><b>Play responsibly.</b> If gambling stops being fun, help is free and confidential.</span>
      <span>National Problem Gambling Helpline: <a href="tel:18006973427"><b>1-800-MY-RESET</b></a> (or 1-800-522-4700)</span>
      <span><a href="#footer">Set deposit &amp; play limits</a> · <a href="#footer">Self-exclude</a></span>
    </div>

    <div class="legal">
      <p><b>Spinzamo is a free-to-play social game intended for entertainment.</b> No purchase is necessary to play or to redeem prizes. A purchase will not increase your chances of winning. Gold Coins have no monetary value and cannot be redeemed. Spinzamo Coins may be redeemed for prizes subject to a 1× playthrough, identity verification, and the Official Rules.</p>
      <p><b>Free entry (AMOE):</b> You can collect free Spinzamo Coins through daily login bonuses and by sending a handwritten request by mail as described in the Official Rules. No purchase or payment of any kind required.</p>
      <p><b>Availability:</b> Spinzamo is available only to eligible individuals aged 21 or older who are physically located in Florida, Illinois, North Carolina, Ohio, or Texas. Void where prohibited. Eligibility, restricted states, prize values, and redemption terms are governed by the Official Rules and Terms of Service.</p>
      <div class="foot-links">
        <a href="#footer">Terms of Service</a>
        <a href="#footer">Official Sweepstakes Rules</a>
        <a href="#footer">Privacy Policy</a>
        <a href="#footer">Responsible Social Gameplay</a>
        <a href="#footer">Contact</a>
      </div>
      <p style="margin-top:14px;color:rgba(255,255,255,.4);">© <span id="yr"></span> Spinzamo. All rights reserved. 21+. No purchase necessary. Terms &amp; Conditions apply.</p>
    </div>
  </div>
</footer>

<!-- ===================== STICKY MOBILE CTA ===================== -->
<div class="sticky-cta" id="stickyCta">
  <a class="btn btn--block" href="#signup">Claim 50,000 Gold Coins + 50 Free Coins</a>
</div>

<script>
/* ============================================================
   CONFIG: set these for your environment
   ============================================================ */
const CONFIG = {
  // Where to send users after they claim (drop them straight into the app / welcome offer / store screen).
  signupUrl: "https://www.spinzamo.com/register",
  // Optional: POST the captured lead to a webhook (Make.com / Salesforce / your CRM). Leave "" to skip.
  leadWebhook: "",
  // Optional: set your Meta Pixel ID to auto-load the pixel + fire events. Leave "" to skip.
  metaPixelId: "",
  minAge: 21,
  availableStates: ["FL","IL","NC","OH","TX"]
};

/* ============================================================
   Meta Pixel (loads only if metaPixelId is set)
   ============================================================ */
(function loadPixel(){
  if(!CONFIG.metaPixelId) return;
  !function(f,b,e,v,n,t,s){if(f.fbq)return;n=f.fbq=function(){n.callMethod?
  n.callMethod.apply(n,arguments):n.queue.push(arguments)};if(!f._fbq)f._fbq=n;
  n.push=n;n.loaded=!0;n.version='2.0';n.queue=[];t=b.createElement(e);t.async=!0;
  t.src=v;s=b.getElementsByTagName(e)[0];s.parentNode.insertBefore(t,s)}(window,
  document,'script','https://connect.facebook.net/en_US/fbevents.js');
  window.fbq('init', CONFIG.metaPixelId);
  window.fbq('track', 'PageView');
})();

function fireEvent(name, params){
  try{ if(window.fbq) window.fbq('track', name, params||{}); }catch(e){}
  try{ window.dataLayer = window.dataLayer || []; window.dataLayer.push(Object.assign({event:name}, params||{})); }catch(e){}
}

/* ============================================================
   UTM capture, preserved through to the signup redirect.
   Email/DOB are NEVER placed in the URL (PII stays in the POST body).
   ============================================================ */
function getUTMs(){
  const p = new URLSearchParams(location.search);
  const keys = ['utm_source','utm_medium','utm_campaign','utm_term','utm_content','utm_creative','fbclid','gclid'];
  const out = {};
  keys.forEach(k => { const v = p.get(k); if(v) out[k] = v; });
  return out;
}
const UTMS = getUTMs();

function buildRedirect(){
  const u = new URL(CONFIG.signupUrl);
  Object.entries(UTMS).forEach(([k,v]) => u.searchParams.set(k,v));
  return u.toString();
}

/* ============================================================
   Age gate (session only, in-memory so the preview keeps working.
   For production, persist the pass with a cookie.)
   ============================================================ */
const gate = document.getElementById('ageGate');
document.getElementById('gateYes').addEventListener('click', () => {
  gate.setAttribute('hidden','');
  document.body.style.overflow = '';
  fireEvent('AgeGatePassed');
});
document.getElementById('gateNo').addEventListener('click', () => {
  document.getElementById('gateDeny').classList.add('show');
});
document.body.style.overflow = 'hidden';

/* ============================================================
   Form submit
   ============================================================ */
const form = document.getElementById('claimForm');
const errBox = document.getElementById('formErr');
const successBox = document.getElementById('formSuccess');
const claimBtn = document.getElementById('claimBtn');

function showErr(msg){ errBox.textContent = msg; errBox.classList.add('show'); }
function clearErr(){ errBox.classList.remove('show'); }

form.addEventListener('submit', async (e) => {
  e.preventDefault();
  clearErr();

  const email = document.getElementById('email').value.trim();
  const state = document.getElementById('state').value;

  if(!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email)) return showErr('Please enter a valid email address.');
  if(state === 'OTHER' || !state) return showErr('Spinzamo isn’t available in your state yet. We’re expanding, check back soon.');
  if(!CONFIG.availableStates.includes(state)) return showErr('Spinzamo isn’t available in your state yet.');

  claimBtn.disabled = true;
  claimBtn.textContent = 'Claiming…';

  const payload = Object.assign({
    email,
    state,
    ts: new Date().toISOString(),
    page: location.href
  }, UTMS);

  // Optional CRM/webhook POST (email stays in body, never the URL)
  if(CONFIG.leadWebhook){
    try{
      await fetch(CONFIG.leadWebhook, {
        method:'POST',
        headers:{'Content-Type':'application/json'},
        body: JSON.stringify(payload),
        mode:'cors'
      });
    }catch(err){ /* fail open, still send the user through */ }
  }

  // Conversion events (the Meta-trackable signal for the broad campaign)
  fireEvent('Lead', {content_name:'spinzamo_welcome', state:state});
  fireEvent('CompleteRegistration', {content_name:'spinzamo_welcome', state:state, status:true});

  // Success state + redirect to the app/store (primes the first purchase / FTD)
  form.style.display = 'none';
  successBox.classList.add('show');
  document.getElementById('successCta').href = buildRedirect();
  setTimeout(() => { location.href = buildRedirect(); }, 1600);
});

/* ============================================================
   Ambient falling coins (decorative, reduced-motion safe)
   ============================================================ */
function spawnCoins(host, count){
  if(!host) return;
  if(window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
  for(let i=0;i<count;i++){
    const el = document.createElement('div');
    el.className = 'fall';
    el.style.left = (Math.random()*100) + '%';
    const size = 22 + Math.random()*30;
    el.style.width = size+'px'; el.style.height = size+'px';
    el.style.animationDuration = (6 + Math.random()*7) + 's';
    el.style.animationDelay = (-Math.random()*8) + 's';
    el.style.opacity = .5 + Math.random()*.4;
    el.innerHTML = '<svg class="coin-svg" viewBox="0 0 64 64"><circle cx="32" cy="32" r="29" fill="#ffbb2e" stroke="#a85e00" stroke-width="2"/><text x="32" y="43" font-family="Poppins" font-weight="900" font-size="30" text-anchor="middle" fill="#8a4b00">$</text></svg>';
    host.appendChild(el);
  }
}
spawnCoins(document.getElementById('heroAmbient'), 9);
spawnCoins(document.getElementById('finalAmbient'), 7);

/* ============================================================
   Sticky mobile CTA (appears after hero scrolls away)
   ============================================================ */
const sticky = document.getElementById('stickyCta');
const heroEl = document.getElementById('signup');
if('IntersectionObserver' in window){
  new IntersectionObserver(([entry]) => {
    sticky.classList.toggle('show', !entry.isIntersecting);
  }, {threshold:0}).observe(heroEl);
}

document.getElementById('yr').textContent = new Date().getFullYear();
</script>
</body>
</html>
